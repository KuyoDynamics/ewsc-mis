schema: ./src/api/domain/schema.graphql
generates:
  ./src/libs/resolvers-types.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers:
          contextType: ../../src/index#GraphQLContext
