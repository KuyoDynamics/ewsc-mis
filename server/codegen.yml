schema: http://localhost:4000/api
generates:
  ./src/libs/resolvers-types.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers:
          contextType: ../../src/utils#GraphQLContext
